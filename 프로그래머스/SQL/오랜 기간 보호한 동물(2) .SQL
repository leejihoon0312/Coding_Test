SELECT RESULT.ANIMAL_ID,	NAME

FROM

(SELECT INS.ANIMAL_ID, INS.NAME , DATEDIFF(OUTS.DATETIME,INS.DATETIME) AS PERIOD

FROM ANIMAL_INS AS INS

JOIN ANIMAL_OUTS AS OUTS

ON OUTS.ANIMAL_ID = INS.ANIMAL_ID

ORDER BY PERIOD DESC

LIMIT 2
) AS RESULT