SELECT ID,	EMAIL,	FIRST_NAME,	LAST_NAME
FROM DEVELOPERS AS D
WHERE EXISTS (SELECT *
            FROM (SELECT * FROM SKILLCODES WHERE CATEGORY = "Front End") AS CODES
            WHERE (D.SKILL_CODE &  CODES.CODE) = CODES.CODE)
ORDER BY  ID    